apiVersion: v1
kind: Namespace
metadata:
  name: degreedops-system
  labels:
    app.kubernetes.io/name: degreedops
    app.kubernetes.io/part-of: degreedops
---
apiVersion: v1
kind: LimitRange
metadata:
  name: degreedops-defaults
  namespace: degreedops-system
spec:
  limits:
    # Backend does synchronous LLM/API calls and can spike; give it headroom.
    - type: Container
      default:
        cpu: "2"
        memory: 2Gi
      defaultRequest:
        cpu: 500m
        memory: 512Mi
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: degreedops-quota
  namespace: degreedops-system
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 8Gi
    requests.storage: 20Gi
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: degreedops-default
  namespace: degreedops-system
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        # Envoy Gateway — routes external requests to backend/frontend.
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: envoy-gateway-system
        # CNPG operator — reaches the degreedops-db pod's healthz for status.
        # Without this the operator reports "Instance Status Extraction
        # Error: HTTP communication issue" and the Cluster never reaches
        # "Cluster in healthy state".
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cnpg-system
        - podSelector: {}
  egress:
    - {}

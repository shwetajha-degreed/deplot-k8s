apiVersion: apps/v1
kind: Deployment
metadata:
  name: deplot-frontend
  namespace: deplot-system
  labels:
    app: deplot-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deplot-frontend
  template:
    metadata:
      labels:
        app: deplot-frontend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: deplot-frontend
          image: dgscucorecr01.azurecr.io/deplot-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            # Next.js writes to .next/cache at runtime; leaving root writable
            # is easier than mounting emptyDirs at every cache path.
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
---
apiVersion: v1
kind: Service
metadata:
  name: deplot-frontend
  namespace: deplot-system
spec:
  type: ClusterIP
  selector:
    app: deplot-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: deplot-frontend
  namespace: deplot-system
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: internal-gateway
      namespace: internal-gateway
      sectionName: https-degreed-com
  hostnames:
    - deplot.internal.sbx.degreed.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: deplot-frontend
          port: 3000
